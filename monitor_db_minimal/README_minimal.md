# MonitorDB Minimal

Versão minimalista com apenas o essencial:
- FastAPI + MongoDB 
- CRUD de clientes e transações
- Sem observabilidade complexa
- Sem integrações externas

## Como usar:
```bash
pip install -r requirements.txt
uvicorn main:app --reload
```